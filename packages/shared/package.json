{
  "name": "@marketpulse/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Domain types shared by the MarketPulse backend and frontend",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
