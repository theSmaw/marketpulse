{
  "name": "@marketpulse/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Domain types shared by the MarketPulse backend and frontend",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsc -b --watch --preserveWatchOutput",
    "build": "tsc -b",
    "test": "echo \"@marketpulse/shared: no tests yet — Story 1.9\"",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
