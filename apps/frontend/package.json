{
  "name": "@marketpulse/frontend",
  "version": "0.0.0",
  "private": true,
  "description": "MarketPulse web client — a skeleton until Story 1.3",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@marketpulse/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
