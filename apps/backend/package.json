{
  "name": "@marketpulse/backend",
  "version": "0.0.0",
  "private": true,
  "description": "MarketPulse API server — a skeleton until Story 1.2",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "sh scripts/dev.sh",
    "build": "tsc -b",
    "test": "echo \"@marketpulse/backend: no tests yet — Story 1.9\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@marketpulse/shared": "workspace:*",
    "fastify": "5.12.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3"
  }
}
