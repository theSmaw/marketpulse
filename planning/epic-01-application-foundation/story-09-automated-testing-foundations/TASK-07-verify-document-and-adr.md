# Task 1.9.7 — Verify, document, and record the decisions as ADR 0009

**Status:** Not started
**Story:** [1.9 Automated Testing Foundations](STORY.md)
**Depends on:** Tasks 1.9.1–1.9.6

## Objective

Close the story: re-run every acceptance criterion from a clean tree rather than inheriting it, remove the three sentences this story makes false, update `CLAUDE.md` and `README.md`, and write down why the testing stack is shaped the way it is.

## Work

- **Re-run the criteria; do not tick them from the task write-ups.** Take each measurement again on the shipping tree. This epic has twice found that a "correction" to a recorded figure was itself wrong — Task 1.7.7 rebuilt four commits to establish it — and a figure that has moved looks exactly like a figure that was mis-recorded. The seven criteria are: a runner in all three packages from the root; backend integration tests through the real HTTP layer including `/health`; frontend component tests through the real tree; example tests of each kind passing; single file and single test by name documented; coverage on demand; conventions documented
- **The three sentences are already dead — verify that, do not repeat it.** "A green `pnpm test` means _no tests exist_, not _tests pass_" was in `README.md`, `CLAUDE.md` and ADR 0001 §5, and **Task 1.9.4 removed all three in one change** rather than leaving them to this task. That was the right call and it is worth reading the reasoning before undoing it: the three became false **simultaneously**, the moment the last `echo` placeholder went, so removing them together there _was_ the single change this story planned for — and leaving two false warnings standing for three more tasks was the exact failure the triplication existed to prevent. ADR 0001 §5 was **amended and dated (2026-09-02) rather than rewritten**, with the record of what was true then kept above the amendment. What this task owes is a check that nothing reintroduced one, and that what replaced them is still true: a green tick means 103 tests pass and specifically **not** coverage
- **Sweep for the other stale claims, and expect the list to be longer than this brief — but check each one first, because Task 1.9.2 already corrected several of them.** It rewrote `CLAUDE.md`'s "`echo` placeholder in all three packages / the **only** placeholder left" sentence, its Commands line, its current-state header and its `packages/shared` file-tree block, and `README.md`'s command-table row and its `pnpm test` warning section — all to say what is _now_ true rather than what will be true at the end. **Task 1.9.3 did the same again** — it rewrote `CLAUDE.md`'s current-state header and its "green `pnpm test`" paragraph to name one remaining placeholder rather than two, its Commands block and its `test`-step timing, `README.md`'s command-table row and warning section, and ADR 0001 §5's consequence paragraph, which had additionally been stale since Story 1.3 in claiming both apps' `dev` were placeholders. **Task 1.9.4 did it a third time**, rewriting the current-state header, the Commands block, the `test`-step timings and the whole "green `pnpm test`" paragraph in `CLAUDE.md`, replacing `README.md`'s warning section with a table of what each package's tests drive, and amending ADR 0001 §5. **Task 1.9.5 did it a fourth time**, and it left less behind than this brief anticipated: it added the `pnpm coverage` rows to `CLAUDE.md`'s Commands block and `README.md`'s command table, wrote the per-package figure table and the no-threshold paragraph into both, amended `CLAUDE.md`'s current-state header and its "green `pnpm test`" paragraph, and added the four coverage paragraphs (the fan-out decision, the provider comparison, the `include` finding, the figures). So the remaining sweep is small and specific: **`CLAUDE.md`'s file tree still has no `coverage/` entry** and no note on the three `vitest.config.ts` files having grown a coverage block — `apps/backend`'s and `apps/frontend`'s test files, both runner configs, `src/test-render.tsx` and `src/test-setup.ts` are already in it; and the closing Commands paragraph's outstanding single-test item should already be gone from Task 1.9.6. **Check before correcting**, and note that the Commands blocks and the figure tables are already right — re-reporting them as this task's own fix is the mistake the paragraph above names. **Check before correcting**, because five of Story 1.8's seven tasks edited that file and re-reporting someone else's fix as your own is this epic's most repeated small mistake — and this story has now done the same thing to itself
- **Write `docs/adr/0009-*` and add its row to `docs/adr/README.md`.** Numbered in the order written and never renumbered. Context, decision, rejected alternatives, consequences — the consequences earn their keep. The decisions this story produces:
  - **The runner**, and what it beat — with the `.js`-extension result under `nodenext` stated as the disqualifying measurement it is, not as a footnote, and with the "already in the lockfile as a Storybook transitive" argument recorded as the weak one it was
  - **How `@marketpulse/shared` is resolved by tests** — settled in Task 1.9.1 as built output through `exports`, so this is recording a decision rather than taking one. The rejected option's failure mode is the interesting half and it is sharper than "diverges from what ships": the source alias stayed **green with `packages/shared/dist` deleted entirely**, and green again with the package's own `exports` map broken — it bypasses the one file that defines the package's contract
  - **That a test file must sit inside a tsconfig's `include`**, because the runner does not typecheck and `tsc -b` is the only thing that does — the same failure mode ADR 0003 records for the Vite dev server, arriving in a second place. Task 1.9.1 has the measurement: a missing required argument ran as two 500s under the runner and was `TS2345` under `tsc -b`
  - **Where the runner is declared and where its environment is** — the root-only rule with the DOM environment as the counter-example that keeps it from being over-applied, exactly as `@types/node` is for `apps/backend`. The DOM environment is **jsdom 30.0.1**, and the rejected alternative was built rather than reasoned about: happy-dom is **cheaper (+7 packages against +22 store entries) and about 3× faster to set up** (environment 130 ms against 380–410 ms), and **no fidelity difference was found in either direction** — both rendered the Base UI popover through its portal, resolved `useId()`-generated `aria-labelledby` identically and produced identical hashed CSS Module class names, with the only difference running the _other_ way (jsdom 30 has no `Element.checkVisibility()`). jsdom was taken on the **failure mode** — a re-implementation's divergences arrive as "the DOM behaved differently from a browser", silent and indistinguishable from a component bug — which is the same argument shape Task 1.8.3 used for `@fastify/cors`. Record the reversal trigger with it: the frontend suite growing to dominate `pnpm test`, where ~275 ms of setup per run stops being free
  - **That a figure's provenance is part of the figure.** Task 1.9.1 recorded jsdom plus Testing Library at "+36 packages"; that is pnpm's `+36 -1` install summary, which counts links into `node_modules` trees. The virtual-store count — this repository's convention everywhere else — is **+22**. Both are true measurements of different things, and quoting one where the other is meant is how the two "corrections" Task 1.7.7 had to rebuild four commits to disprove happened
  - **That `globals: false` has a consequence nobody predicted, and it belongs beside the decision rather than in a footnote.** `@testing-library/react` registers its own `afterEach(cleanup)` only when it can see a global `afterEach`, so turning `globals` off to protect `apps/frontend`'s tsconfig `types` array silently disables it. Measured: `document.body` held 1 and then 2 children across two tests, surfacing later as `getByRole` throwing "found multiple elements" in a test that did nothing wrong. A `setupFiles` entry is what replaces it — deliberately not the render helper, because a leaf-component test calls `render()` directly and needs the cleanup just as much
  - **Where tests live and what they are named**, and what that buys: inside `src/` they typecheck, lint type-aware, and take the React Compiler rules and `--max-warnings 0`. Record it as **forced rather than preferred** — the rejected alternative is a `tsconfig.test.json` with `noEmit`, which typechecks correctly and fails `pnpm lint` on every test file, because ESLint's project service only ever discovers a `tsconfig.json`. The consequence it buys is a real one and belongs beside it: tests emit into `dist/` in the two packages that emit at all
  - **Where the runner's configuration lives, and why there is no root config** — one `vitest.config.ts` per package, because root `test` is a `pnpm -r` fan-out and a root `projects` list would be the second command meaning "run the tests" that this story was told not to introduce. This is the one place the testing stack deliberately departs from `eslint.config.mjs` and `prettier.config.mjs` being single root files, and the reason those two do not share is worth stating: their tools run **once** from the root and this one does not. The `.mts` finding belongs here as the conditional it now is — it applies only if a root config is ever added
  - **That Vitest 4 excludes only `node_modules` and `.git` by default**, so each package scopes `test.include` to `src`. It is the story's sharpest small finding and it is an allowlist for the same reason `config.ts` rejected `redact`. Without it a populated `dist/` runs every test twice, against the last build rather than the current source — measured at 4 files and 14 tests against the 2 and 7 that existed
  - **That `globals` is off and the matchers are imported by name**, because the alternative is a `"vitest/globals"` entry in every package's tsconfig `types` array — and `apps/frontend`'s explicit array is the browser boundary's last stated guarantee
  - **The one render helper** as the third and last description of the application's context, after `App.tsx` and `.storybook/preview.tsx`, and the rule that every provider Epic 2 adds lands there
  - **Coverage on demand and no threshold**, with the baseline figures and what they structurally exclude — and there are four decisions inside it rather than one. **It fans out**: `pnpm coverage` is `pnpm -r run coverage` and each package's script is its own `test` with `--coverage` appended, so the "no root Vitest config" decision above holds for a second reason; running once from the root was rejected because it means creating the `projects` list Task 1.9.2 declined, in exchange for one merged percentage over three packages that share no code. **The provider is `@vitest/coverage-v8` and `@vitest/coverage-istanbul` was installed and measured before being rejected** — from an identical clean baseline, +31 store entries / +20 MB / +110 lockfile lines against +29 / +20 MB / +94, neither tripping `allowBuilds`, and no meaningful runtime difference; they agree exactly on `packages/shared` and differ on `apps/frontend` by **one branch**, because istanbul counts a default parameter (`compact = false` in `ErrorFallback.tsx`) and v8 does not. v8 was taken because it does not rewrite the sources it measures, which matters most in the one package whose Vitest config **is** its build config — and that undercount is the recorded reversal trigger. **The exclusions each carry their reason in the config**, and the two omissions from that list are the decision worth recording: `apps/backend/src/index.ts` and `apps/frontend/src/main.tsx` are **deliberately left in the denominator at 0%**, so the process half Story 1.10 owns is visible as a figure rather than as a caveat. **And no threshold**, with Story 1.10 named as who could set one against 30.00% / 64.33% / 68.25% of statements
  - **That Vitest 4's coverage defaults are the second half of the `defaultExclude` finding above, and sharper.** `coverageConfigDefaults.exclude` is **`[]`** and `coverage.include` is **undefined**, meaning "only the files some test loaded" — so at the defaults `packages/shared` reports **100% with an empty file table**, because nothing imports `ticker.ts`, `anomaly.ts` or `feed-status.ts`. An explicit `include` scoped to `src` is what makes the number describe the package rather than the subset a test happened to reach, and it is the same allowlist argument arriving in a third place
  - **`@storybook/addon-vitest`, rejected — and the interesting half is that this story's predicted blocker did not materialise.** Story 1.9 expected `@vitest/browser` and a Playwright download. Measured: **+1 package**, no Playwright, no `@vitest/browser`, no install script, and it **runs under jsdom** giving 47 story smoke tests — provided `css: true`, because `.storybook/preview.tsx` calls `getTokens()` and that throws with no stylesheet applied; `css: true` itself costs nothing measurable. So the rejection rests on **shape rather than cost**: a `projects` array making `apps/frontend` the only package with two test projects, a second source for coverage to reconcile, and stories that are a workshop rather than assertions — `AppHeader`'s `AllPermutations` has two a11y rules disabled precisely because a grid of six banners is not a page. Reversal trigger: a component shipping without a written test, or Epic 15 wanting axe to fail rather than report
  - **That the test environment applies no global stylesheet**, so `getComputedStyle` returns nothing and `getTokens()` throws — which makes "never assert on colour" a structural impossibility rather than a discipline, and is also what would have broken the Storybook addon. CSS **Modules** are real, though, because the frontend's config merges `vite.config.ts`: a rendered component carries Vite's own hashed class names rather than an identity proxy
  - **The two reversal triggers Task 1.9.1 recorded**, because a decision without one is an assertion: the frontend leaving Vite, which unwinds the argument that carried the choice; and the backend suite growing to dominate `pnpm test`, where `node:test` against **built** output is the fallback for the two packages that emit — at the price of two runners
  - **That a test can close a gap no `verify` step could**, which is the story's one structural finding beyond its conventions. Task 1.9.3's route-schema audit walks the route table through an `onRoute` hook added after `buildServer()` returns — deferred plugin registration is what makes that work — and it found `OPTIONS *`, a route `@fastify/cors` registers and nobody here wrote. Record the rule and its limit: `setNotFoundHandler` is not a route and can never carry a response schema, so `apiError()`'s four-slot object is what holds there
  - **What this story deliberately did not test**, which is the entry a future reader will most want: axe, which was considered for the frontend suite and not adopted — Epic 15 owns the accessibility review and Task 1.5.4's figures are still the record; and the backend's process half — signals, exit codes, the shutdown ceiling, the second-signal path, `EADDRINUSE`, both crash handlers — needs a child process against a built tree, and the frontend has no async subject at all until Story 1.12's polling effect. Task 1.9.3 settled this as **out of scope with Story 1.10 as its owner** rather than half-demonstrating one, and the argument belongs beside the decision: such a test needs a build to have run, spawns and signals processes, and its first failure mode is a port held by a previous run, so a single one of them in the same `vitest run` as 48 injected tests makes the fast suite conditional on a build and occasionally flaky
- **Update `EPIC.md`.** Mark Story 1.9 complete, and do the pass the last five stories each did — noting that this story hands Story 1.10 a second thing beyond CI: the backend's untestable-by-injection process half, with 1.10 named as its owner rather than merely as the story after this one. Then: read Stories 1.10, 1.11 and 1.12 against what this story actually shipped rather than what it was expected to ship, and correct the predictions it falsified. Story 1.10 is the direct consumer — it depends on 1.9 and it inherits a `pnpm test` that finally means something, a coverage command that is deliberately **not** in `verify`, and the standing instruction that `pnpm ready` must not become a `verify` step either. Say whether anything this story found should change what CI runs. Do not add, delete or re-order a story without saying why in the epic's own notes, as every previous pass has
- **Re-take the workspace's own figures, because they are in two documents.** `pnpm verify` warm and from a cold clone, per-step — the split is what means something, since four consecutive stories have now measured the total going up and down while the tree only grew. The new `test` step is the figure this story owns. Take the built artefact's module count, byte size, file count and md5 too. Two of this brief's guesses are now settled: the frontend gained a DOM environment as a devDependency and **no Storybook test addon**, and the coverage provider is a **root** devDependency beside `vitest` rather than a per-package one — no package imports it, the runner resolves it, and all three packages need it, which is the opposite of `jsdom`'s case. The artefact has been byte-identical at 271 modules / 343,658 B / 10,926 B / three files / md5 `cba2825c…` since Task 1.7.7 and was still identical after Task 1.9.5; if it has moved, attribute it
- **Run the clean clone.** It is the ninth, and its new question is already half-answered by measurement rather than by reasoning — confirm it rather than re-derive it. **`pnpm test` does not pass before a build, and it fails loudly**: with `packages/shared/dist` moved aside, `apps/frontend` reports `Failed to resolve import "@marketpulse/shared" from "src/routes/MarketOverview.tsx"` and `apps/backend` reports `Failed to resolve entry for package "@marketpulse/shared"`. Both name the package, which is the good case. **The dangerous case is a _stale_ dist rather than a missing one**, and it was measured too: removing one export from the built `index.js` without rebuilding gave **13 failing backend tests whose messages name nothing about staleness** — they fail on values. One detail to carry into any CI ordering: the backend still reported **16 passed** in the missing-dist run, because `config.test.ts` imports nothing shared, so a partial pass is what this looks like from a distance. `verify` orders the build already; a bare `pnpm test` does not. Record the install's package count — 327 at Task 1.8.7, and this story adds to it
- **Say what this story did not close.** The `verify` gaps are unchanged unless this story changed them, and one of them is now testable in a way it was not: `apps/backend/scripts/dev.sh` is still read by nothing, the two `clean` scripts still carry unchecked `rm -rf` fragments, `README.md`'s prose figures and links are still read by no tool — and Task 1.9.5 **enlarged** that one rather than leaving it level, by publishing a three-row table of coverage percentages into both `README.md` and `CLAUDE.md` that nothing regenerates and nothing checks; re-take those figures here rather than citing them, and say plainly that they go stale the moment a test is added. A stated invariant can still quietly stop being enforced. Re-date them rather than deleting them. **Task 1.9.3's route-schema test landed**, so that is one gap closed by a test rather than a `verify` step — say so, because it is the model for the others, and say what it caught on its first run. **One gap this story opened rather than closed, and it should be recorded rather than left to be discovered:** `packages/shared` and `apps/backend` now emit a compiled copy of every test file into `dist/`, unreachable to a consumer through the `exports` map and invisible to the runner through the scoped `include`, but real — and the `tsc -b --clean` orphan rule applies to them, so a deleted test file leaves four files behind permanently
- Run `pnpm format` then `pnpm verify` from a clean tree, and confirm the artefact is reproducible

## Done when

- Every acceptance criterion is re-run on the shipping tree, and each one is ticked, annotated, or handed to a named story with its constraints
- Every figure this story publishes was re-taken here rather than copied from a task file
- All three "no tests exist" sentences are gone in this same change, ADR 0001 §5 amended rather than rewritten
- `docs/adr/0009-*` exists, its row is in `docs/adr/README.md`, and each decision names the alternative it beat
- `CLAUDE.md` and `README.md` agree, and both describe a workspace that has tests
- `EPIC.md` marks Story 1.9 complete and records the pass over Stories 1.10–1.12
- A clean clone installs, builds and passes `pnpm test`, with the per-step `verify` split recorded

## Notes

The two things most likely to be got wrong here are citing a figure instead of re-taking it, and ticking a criterion an earlier story annotated. Both have precedent in this epic and both were caught late. The third, specific to this story: writing "tests pass" where the honest sentence is which tests exist. The whole point of removing the placeholder warning is that the green tick now means something — it means precisely what was written, and no more.
