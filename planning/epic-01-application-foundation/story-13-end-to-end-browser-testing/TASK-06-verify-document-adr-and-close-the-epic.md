# Task 1.13.6 — Verify, document, record ADR 0013, and close Epic 1

**Status:** Not started
**Story:** [1.13 End-to-End Browser Testing](STORY.md)
**Depends on:** Task 1.13.5

## Objective

Close the story the way every Epic 1 story has been closed — re-running every criterion and re-taking every figure from a clean tree — and then close the epic, which no task before this one has had to do.

## Work

- **Re-run all six acceptance criteria and re-take every figure rather than citing one.** The record across twelve stories is that the half that is cheap to check has never been wrong and the half that cannot be checked has been wrong nearly every time it is read
- **Write `docs/adr/0013-*`, and note that it now has TWO suites to account for rather than one (amended 2026-09-04, after Task 1.13.5).** `pnpm e2e` runs before a merge against a local pair and gates it; `pnpm e2e:deployed` runs after one against production and gates nothing. They share `support/app.ts` and `support/axe.ts` and share neither their config, their target, nor how they learn where that target is — and **`what a green run certifies` is therefore two lists, not one**, which is the shape `e2e/README.md` already carries and the ADR should follow rather than merge. It should record decisions rather than a changelog: the tool and what the rejected one was better at; where specs live and why a `.ts` file's home is a constraint rather than a preference; where the suite sits relative to `pnpm verify`, stated **next to** Story 1.10's rule that the pipeline defines nothing of its own, because this is the first thing that strained it — **Task 1.13.4 answered that and wrote the argument into the workflow beside the job (2026-09-04), so the ADR's job is to record the decision and its two rejections, not to re-derive them**: a second job in `verify.yml`, in parallel with the chain, gating a merge, invoking `pnpm build`, `pnpm dev` and `pnpm e2e` by name; what the suite deliberately does not assert; and — in the shape ADR 0010 used for the tick — **what a green run certifies and what it does not**. It must also record the ~~**third-kind invariant this story created**~~ ~~**two third-kind invariants this story created (amended 2026-09-04)**~~ ~~**FOUR third-kind invariants this story created (amended again 2026-09-04, after Task 1.13.4)**~~ **SIX, after Task 1.13.5 (amended again 2026-09-04)**. The first: the `e2e` package joins every `pnpm -r` fan-out automatically, and the _only_ thing keeping the browser suite out of `pnpm test` is that the package has no `test` script. Nothing checks that it stays absent, `package.json` cannot hold a comment saying why, and the failure mode is that `pnpm test` silently starts needing two servers and a build — the exact outcome Story 1.9 and Task 1.10.5 spent two tasks preventing. **Decide explicitly whether that stays prose or becomes a check**, and if it stays prose, say where the durable copy is. The second, added by Task 1.13.3: **`e2e` pins `axe-core` at 4.13.0 to match the version `@storybook/addon-a11y` resolves, and nothing checks that pair** — the frontend's copy arrives transitively, so there is no manifest for the two to be compared in, and the failure mode is the workshop and the browser suite reporting different numbers for the same page, which makes both untrustworthy rather than one of them wrong. Same treatment question, same answer required.

  **The third and fourth were created by Task 1.13.4 and the fourth is the sharpest one in the story.** The third: **the ruleset now requires two checks keyed on two job names**, `verify` and `e2e`, so renaming either job in `verify.yml` un-requires it silently — the same failure Task 1.10.2 settled names early to avoid, now doubled — and the `e2e` job additionally **restores the pnpm store cache and never saves it**, so it depends on the `verify` job continuing to save it and would silently install cold forever if that step were removed. The fourth: **the axe gate's whole value depends on the browser computing real styles, and nothing checks that it does.** Task 1.13.4 confirmed it with a throwaway probe — the page ground exact, `color-contrast` passing on 65 nodes — and **deleted the probe**, so the property is prose. The failure mode is the one a green run structurally cannot distinguish from success: a renderer that skipped style computation turns the gate green by making it blind. **Decide explicitly between prose and a check here rather than defaulting to prose**, because unlike the other three there is a cheap check available — `expectNoAxeViolations` could assert that `color-contrast` appears in `passes` with more than zero nodes, one line beside the assertion it protects, which is this repository's own rule that a test beats a `verify` step when the thing being checked is reachable from an assembled instance — **and note that as of Task 1.13.5 that one-line check would have to go in TWO places, because `reportAxe` is a second consumer of the same injected axe and a check added only to `expectNoAxeViolations` leaves the deployed reading blind**

  **The fifth and sixth were created by Task 1.13.5, and the sixth is a blind spot rather than a dependency (added 2026-09-04).** The fifth: **the `check-deployed` job restores the browser cache and never saves it**, so it depends on `verify`'s `e2e` job continuing to save it under the same key — the same shape as the pnpm-store dependency in the third, now doubled and now spanning two workflows. It behaved exactly as intended on its first run (an exact-key hit, no second saver, no `Cache already exists` warning), which is the point: the failure mode is silent and slow rather than loud. The sixth: **the deployed check compares its two inputs against the live environment and cannot detect both inputs being wrong in the same direction.** Pointed at a stale `FRONTEND_ORIGIN` with a matching `VITE_API_BASE_URL`, it would pass — green — against the wrong site entirely. That is a real limit of an instrument whose whole value is catching values that disagree, and it belongs in what a green deployed run does not certify.

  **Two more entries of this family that are NOT new invariants and should be recorded as the counter-examples they are.** `scripts/run-deployed-check.mjs` now **guards on the presence of `packages/shared/dist`** with a message naming the command — presence and not freshness, the shape Task 1.10.5 settled after building a staleness check and removing it — so that is a stated invariant this story turned into a check. And the **three-values-must-agree** invariant (`VITE_API_BASE_URL` in `deploy.yml`, the Static Web App's hostname, `CORS_ORIGIN` on the Container App) has existed unnamed since Story 1.11 and is **the first invariant of this kind in the repository that a check now closes rather than prose describing**. The ADR should carry both, because the interesting claim is not that this repository has six unchecked invariants — it is what distinguishes the ones worth checking from the ones worth writing down, and this story now has examples on both sides.

  The ADR also owes **two declines with their reversal triggers**, both taken by Task 1.13.3 and both the kind that read as omissions if unstated: a **render-failure journey** (this application contains no way to produce one, and neither of this repository's two precedents transfers without either shipping a throwing route or making the artefact under test not the artefact that ships — so `getByRole('alert')` at count zero on every page is what is asserted instead; reversed by Epic 4's first real data or Epic 6's canvas), and a **hung-socket journey** (36 s to assert a distinction the user cannot see, against a deadline Task 1.12.6 already proved wired in a browser; reversed by a change to `API_TIMEOUT_MS`)

  **Task 1.13.5 added three more declines and one decision that reads as an inconsistency unless it is argued (added 2026-09-04).** The declines: **a `schedule:` on the post-deploy check** (that would make it uptime monitoring, which nothing in the roadmap owns, and it has a bill — a green run costs the deployed backend +5 requests against an idle baseline of 4 per 30 s, negligible once per merge and not negligible on a timer); **adding it to the ruleset** (it runs after a merge, so requiring it would gate on something that cannot have happened yet — the same reason `deploy` is not required, and a decision to state because a reader finding it unrequired cannot otherwise tell that from a forgotten step); and **reproducing the mixed-content half of the `VITE_API_BASE_URL` failure against production**, which was declined on the user's instruction and whose gap is named rather than hidden — the failure was produced at the artefact and caught, and only the browser's block itself went unreproduced, because the only HTTPS host available is production. The decision that needs arguing rather than stating: **axe is a gate before the merge and a REPORT after it**, with the reversal trigger being a divergence between the two readings. It is the same rules pointed at the same source, treated two ways, and the argument is that a red post-deploy result is a rollback decision while a contrast ratio is not a rollback

- **Update `CLAUDE.md` and `README.md` together, and note that Task 1.13.2 already made three of the edits this bullet asks for — so re-check them rather than re-making them (amended 2026-09-04).** The tree diagram already carries the fourth workspace package at `e2e/`, `pnpm-workspace.yaml` already carries the glob, the root solution file already carries the reference, and the Commands sections of both files already carry `pnpm e2e`. What is **not** done is the test-level list, which is still written as four levels. Two files Task 1.13.2 added are in neither document's prose: **`scripts/pair-addresses.mjs`**, which is now the one definition of where the pair is, and the **refactor of `check-ready.mjs` onto it** — `README.md`'s `pnpm ready` section still describes that script as reading the built config itself, which is true only indirectly now. The test-level list is now five levels rather than four; ~~`allowBuilds` has a second entry for the first time since Task 1.4.5~~ — **that prediction is false and the opposite claim needs no edit at all (amended 2026-09-04): Playwright ships no install script, so `esbuild` is still `allowBuilds`' only entry and Task 1.4.5 is still the only time the policy has fired.** Re-run the sweep and say so rather than leaving the reader to infer it from silence. ~~The pinned-action count moves if Task 1.13.4 added one.~~ **It added none and the count still moved (amended 2026-09-04): the `e2e` job reuses `actions/checkout`, `actions/setup-node`, `actions/cache`, `actions/cache/restore` and `actions/upload-artifact`, so it is still five distinct actions but now across **thirteen** uses and **six** distinct `uses:` references** — `actions/cache/restore` being a sub-action of `actions/cache` at the same SHA is a shape the recorded count has never had to describe before. Re-count out of `.github/workflows/` rather than copying that. And the fourth workspace package is **the first structural change to that tree diagram since Story 1.1 drew it** — three edits rather than one, because `pnpm-workspace.yaml` gains a glob and the root solution file gains a reference — so re-read all three and confirm they agree with the tree rather than trusting that they were made correctly.

  **Task 1.13.3 made three more of these edits and added two files that need prose (amended 2026-09-04).** `CLAUDE.md`'s tree now carries `e2e/README.md`, `e2e/support/` and the `axe-core` pin, and its Commands entry for `pnpm e2e` carries the suite's shape — re-check those rather than re-making them. What is **still** missing from both documents is the test-level list (four levels, should be five) and `scripts/pair-addresses.mjs`. New since that bullet was written: **`e2e/README.md` is where the suite's rules now live** — the must-not-assert list, the workers decision, the axe decision, the derivation rule and what a green run does not certify — so `README.md`'s and `CLAUDE.md`'s job is to _point_ at it rather than to duplicate it, and duplicating it is exactly how this repository's twelve-block sweep problem started.

  **Task 1.13.4 made a fourth round of these edits and enlarged the prose gap in the place it is most read (amended 2026-09-04).** `README.md`'s **badge paragraph** — the most-read prose in the repository, sitting under a green tick — now claims the browser suite too, and its `pnpm e2e` section now carries runner figures (2 workers, 872,142 B and 577 B artefacts, 7-day retention) that nothing reads. `CLAUDE.md`'s tree entry for `verify.yml` now describes two jobs, its pinned-action count moved, and its ruleset paragraph names two required checks. `docs/adr/0010-*` §17 was amended in place rather than left stale, which is the treatment Task 1.12.8 established. Re-check all of those against the tree; the test-level list is **still** four levels in both documents and `scripts/pair-addresses.mjs` is **still** in neither

  **Task 1.13.5 made a fifth round, and it turned the test-level question from arithmetic into a decision (amended 2026-09-04).** Already done and to be re-checked rather than re-made: both Commands sections carry `pnpm e2e:deployed`; `CLAUDE.md`'s tree carries `scripts/check-deployed.mjs`, `scripts/run-deployed-check.mjs`, `e2e/playwright.deployed.config.ts`, `e2e/specs-deployed/` and `e2e/support/deployed.ts`, and its `deploy.yml` entry now describes two jobs; `README.md` has a `pnpm e2e:deployed` section; and `e2e/README.md` carries the post-deploy sections, so both root documents' job is still to **point** at it rather than duplicate it. **The pinned-action count moved again** — `check-deployed` adds uses of `actions/checkout`, `actions/setup-node`, `actions/cache/restore` (twice) and `actions/upload-artifact` — so it is **still five distinct actions** and the use count is higher again; re-count out of `.github/workflows/` rather than copying any number. And the open item is no longer "four levels should be five": **is the deployed check a SIXTH level, or the fifth level pointed at a different target?** It asserts things the local suite structurally cannot — a host's `navigationFallback`, a real platform-only `CORS_ORIGIN`, an artefact that is not the one `verify` fingerprinted — which argues for six; it runs the same tool over the same helpers, which argues for five-with-two-targets. Decide it explicitly, because whichever answer is taken has to be written the same way in both documents

- **Run the duplicated-sentence sweep with a `grep`, and expect this story to have created work for it.** Two stories have found this class of drift: Task 1.9.7 found a warning standing in **thirteen** places rather than the three recorded, and Task 1.10.8 found **eight** copies of a claim false since Task 1.10.5 and amended only in the copy being edited. **This story's own STORY.md added a copy of the "Conventions from Story 1.1" block**, so the hash comparison should now find ten identical rather than nine — verify that rather than assuming it, since a block copied by hand is exactly how the count starts drifting. **Re-measured in Story 1.12's review pass (2026-09-04) and the shape is worth knowing before you count: there are twelve blocks, of which ten are byte-identical.** Stories 1.2 and 1.3 carry their own historical strike-throughs and Story 1.1 correctly carries none, having set them — so the number to compare is _identical hashes_, not copies, and the two are not the same number
- **Sweep for claims this story falsified, and note that one candidate on the original list came off it.** Every statement that the backend's process half is the fourth and last level of test; ADR 0009's account of what has and has not been installed, which currently records that **no** Playwright was ever pulled in — that one is now false and is the single largest correction this story owes ADR 0009. ~~And the "`esbuild` is the only install script in the tree" sentence, which stands in several places and is about to be false~~ — **it is still true, so those copies need no edit; count them anyway and record that they were checked**, because a sentence verified and left alone is a different record from a sentence nobody read.

  **Two more were found in this story's own STORY.md while feeding Task 1.13.3's findings back, and the lesson is the one this bullet exists for (amended 2026-09-04).** Its "What that means for this story" section still asserted that **`allowBuilds` will almost certainly fire** — falsified by Task 1.13.1 and again by 1.13.2 — and its Story 1.3 hand-over still said `vite preview`'s fallback answers **any** unmatched path with a 200, which Task 1.12.6 corrected to an `Accept`-header rule _before this story started_. Both are amended in place. **A story's own hand-over sections rot exactly as the shared documents do**, and nothing has ever swept them; add them to whatever this task sweeps

  **Task 1.13.5 falsified the largest multi-copy claim this story has produced, and it has already been counted (added 2026-09-04).** _"`curl` is **structurally incapable** of catching a wrong `CORS_ORIGIN`"_ stands at **14 occurrences across 12 files** — `CLAUDE.md`, `EPIC.md` (twice), `docs/adr/0012-*`, `HOSTING.md`, four task files and three STORY.mds, plus the two in this story's own new code that already carry the correction. It is **too strong**: the status, the body and the log genuinely cannot catch it, but `access-control-allow-origin` is a readable copy of `CORS_ORIGIN` — `@fastify/cors` with a string origin asserts the configured value unconditionally — so an instrument **told the frontend's origin** can compare them. That value is exactly what no server-side instrument has, and the comparison is a proxy for the browser's verdict rather than the verdict, saying nothing about the second failure where the backend is never asked. This is a **scoping clause and not a strike-through**, the treatment the non-atomic-upload claim got across fourteen files. **And the 14 is a grep count, not a copy count** — some of those occurrences are about other things entirely — which is precisely Task 1.10.8's lesson that a sentence duplicated for legibility has to be read rather than replaced.

  **Two smaller ones from the same task.** Every statement that Story 1.5's deep-link and missing-asset criteria _"were closed by hand in Task 1.11.4 and have been checked by a person or not at all ever since"_ is now false — `e2e/specs-deployed/host-routing.spec.ts` asserts them on every deploy. And every statement that the browser suite _"has never spoken to Azure"_ or that _"nothing in this repository drives the deployed environment"_ needs re-reading: one suite still has not and one now does

- **A sweep this story created rather than inherited, and it touches every accessibility figure in the tree (added by Task 1.13.4, 2026-09-04).** The axe gate's first CI run reported `scrollable-region-focusable` on the landing route — a real WCAG 2.1.1 defect standing since Story 1.5, invisible on the development machine because **the rule does not fire while the scrolling box happens to contain something focusable**, and reproducible locally at a viewport 160 px shorter. Every axe measurement this repository records — Tasks 1.4.5, 1.5.4, 1.7.6, 1.12.4, 1.12.5, 1.13.3 — was taken at **one viewport**, and none of them says so. That is a **scoping clause** across all of them, the treatment the non-atomic-upload claim got across fourteen files, not a strike-through: each figure was right for the window it was taken in. The transferable sentence is that _0 violations at one viewport is not 0 violations_, and the ADR should carry it as part of what a green run does not certify
- **Two figures this story corrected that live outside it, and both are the kind a citation carries forward.** The **poll cycle**: Tasks 1.12.6 and 1.12.7 record **31.00 / 31.05 s** and Task 1.13.1 measured **30.02 s** through Playwright — the recorded figures are right about a driven Chrome tab and wrong about this runner, so they need a **scoping clause rather than a strike-through**, the treatment the non-atomic-upload claim got across fourteen files. And the **virtual-store baseline**: the working tree read **425** entries where a fresh install reads **400**, because pnpm never prunes and `pnpm prune` removes nothing — so **a store-entry count is only comparable across a fresh install**, which is a rule this file does not currently state and which affects every `+N entries` figure recorded since Story 1.9. **A third, from Task 1.13.4: the runner now installs `Packages: +400`**, which supersedes Task 1.10.1's 397-on-Linux/398-on-macOS pair — Story 1.13's additions moved it, and that pair is quoted in more than one place
- **A third figure this story corrected, and it is the one most likely to be carried forward wrong (added by Task 1.13.3, 2026-09-04).** The `vite preview` claim above and this one are the same class: `CLAUDE.md` and this story's STORY.md both stated that `preview`'s SPA fallback answers **any** unmatched path with `index.html`, and Task 1.12.6 had already corrected it to an `Accept`-header rule. The deployed host's `navigationFallback` is a **URL-pattern** rule and behaves the opposite way on the one request an API client actually sends. **Three hosts, three behaviours** — `python3 -m http.server` 404s both, `vite preview` splits on `Accept`, Static Web Apps splits on path — and every sentence in the tree that describes "the SPA fallback" without saying which host it means is wrong about two of them. Sweep for that phrasing rather than for the number
- **The `verify`-gap list needs re-reading rather than extending, and the open question in it is now answered (amended 2026-09-04).** The guess was that a browser tool's config file would be a fifth instance of the kind whose formatting is checked and whose schema is not. **It is not, and Task 1.13.2 measured it rather than assuming it**: `playwright.config.ts` sits inside `e2e/`, whose tsconfig `include` is `**/*.ts`, so `--print-config` reports **168 rules** on it — identical to a covered source file — with `no-floating-promises` at error, and Prettier formats it. It is the **first tool configuration file in this repository that is not a gap of any kind**, which is a more interesting entry than another one that is, and the one line holding that property is the `include` in `e2e/tsconfig.json`. Record it as an entry that was checked and found closed. There is, however, a **new third-kind gap this story definitely created** — see the ADR bullet.

  **Task 1.13.5 added a second instance of the closed kind and enlarged one open entry (amended 2026-09-04).** `e2e/playwright.deployed.config.ts` sits inside the same `**/*.ts` include, so it is covered exactly as `playwright.config.ts` is — a second file that is not a gap of any kind, which is worth recording because two instances make it a property of the `include` rather than a coincidence. What did grow is the workflow entry: `deploy.yml` now carries **two jobs**, and its schema is still checked by nothing, so a misspelled key in `check-deployed` is green locally and red only on a runner — the same declined-`actionlint` answer, now guarding more. The two new `scripts/*.mjs` files are **not** gaps: ESLint and Prettier read them, verified

- **Close Epic 1.** Check each of `EPIC.md`'s four exit criteria against the tree rather than against story statuses — the last of them was met by Story 1.12, and nothing else in the epic will check them. Record any criterion met by a different mechanism than the one it names, and any not met. **One of them changed how it is evidenced rather than whether it is met (amended 2026-09-04, after Task 1.13.5):** Story 1.5's deep-link and missing-asset criteria were met by a person in a browser in Task 1.11.4 and are now asserted by `e2e/specs-deployed/host-routing.spec.ts` on every deploy, so the epic can cite a check rather than a reading — which is exactly the sort of "met by a different mechanism than the one it names" this bullet asks to record
- **Hand over to Epic 2 deliberately**, and say what this suite is not: it is a harness with a handful of journeys, not coverage of an application that barely exists. Epic 8 is the checkpoint with journeys worth asserting on in quantity, and the value it inherits is that the tool, the home and the CI position are already decided and recorded. The roadmap divergence this story deliberately did not resolve — `PRODUCT_SPEC.md` §41 puts E2E tests in Phase 6 while Epic 15's scope carries only "Testing strategy documentation" — should be handed on as an open question rather than left in this story's notes
- Re-run the full chain from a **clean clone** with an empty pnpm store, as every closing task in this epic has: install, `pnpm verify`, the per-step split, the artefact hashes and the test counts across every suite — now including this one, and including how much longer a clean clone takes when it also has to download a browser. **Include `pnpm e2e:deployed` in that run and expect it to be the first command in this repository a clean clone can point at something outside itself (added 2026-09-04).** It needs no pair and no `pnpm build`, but it does need `packages/shared` built — the failure that took the `check-deployed` job red on its first execution, invisible on any machine with a built tree — so a clean clone is the one place its guard actually fires, and running it there is how that guard gets tested rather than assumed

## Done when

- All six criteria are met, each re-verified rather than inherited, with the deployed evidence separate from the local evidence
- `docs/adr/0013-*` exists and records decisions, including what a green run does not certify
- `CLAUDE.md` and `README.md` agree with the tree, and the sweep has been run with a `grep` and its counts recorded
- Epic 1's four exit criteria have each been checked against the tree, with any gap named
- The axe gate's dependence on a browser that computes styles is either checked or written down as prose with its durable copy named, and the choice is argued
- The `curl`-is-structurally-incapable correction has been swept across all 14 occurrences, as a scoping clause rather than a strike-through, with each occurrence read rather than replaced
- Whether the deployed check is a sixth test level or the fifth pointed at a second target is decided, and written the same way in both documents
- `pnpm verify` passes from a clean clone, the pipeline is green, and the deployed environment is up and correct — with `pnpm e2e:deployed` run from that clean clone, which is the only place its built-output guard actually fires

## Approach note

Every closing task in this epic has found at least one recorded claim that had stopped being true, and every one of them was found by re-measuring rather than by reading — Task 1.11.8 found three, including a paragraph that contradicted itself two sentences apart and an arithmetic error of a factor of about 35. This task closes an epic as well as a story, so it is reading **twelve** stories' worth of claims rather than one. Budget for it.

**It grew again with Task 1.13.5 and it is now the largest closing task in the epic (amended 2026-09-04).** It owes an ADR covering **two** suites, six third-kind invariants plus two counter-examples, four sweeps of which one has 14 known occurrences, and an epic close. **Splitting it was considered and rejected**, and the reason is worth stating rather than leaving as an omission: every story in this epic has closed with exactly one task, and a closing task exists to re-verify a whole story at once — split in two, each half re-verifies part of a story and neither is the re-verification the pattern is for. The epic close genuinely depends on the story close, so a separate "close Epic 1" task would be a task that cannot start until the other finishes and shares all of its context. One task, budgeted honestly, beats two that pretend to be independent.
