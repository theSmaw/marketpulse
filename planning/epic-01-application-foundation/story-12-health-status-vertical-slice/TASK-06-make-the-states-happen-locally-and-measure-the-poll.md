# Task 1.12.6 — Make all three states happen locally, prove recovery, and measure what the poll costs

**Status:** Complete (2026-09-04)
**Story:** [1.12 Health & Status Vertical Slice](STORY.md)
**Depends on:** Task 1.12.5 — complete, and this file was amended again against what 1.12.5 measured on 2026-09-04

## Objective

Produce every state by a real cause rather than by a flag, watch recovery happen without a reload, and put numbers under "polling is deliberate about frequency and does not spam logs". Local only; the deployed environment is Task 1.12.7's.

## Work

- **Produce each state by a cause, and write down the cause beside the reading.** Unreachable: stop the backend, and separately hold port 3000 with a squatter — which is the state `pnpm dev` leaves you in while looking entirely healthy, sixteen log lines scrolled away behind Vite's banner and nothing exiting non-zero. Degraded: Task 1.12.1 defined it structurally and named two producible causes, so there are two readings to take rather than one — `not-ok-status`, by pointing `VITE_API_BASE_URL` at something that answers non-2xx, which reaches the client as **either** `api-error` or `http-error` depending on whether the body is the contract's own error shape and collapses to the same state either way, and `unreadable-body`, by pointing it at any static host, which answers `index.html` at **200** and is the `vite preview` trap this repository has measured twice. Healthy: the ordinary pair. **Task 1.12.4 made the two degraded causes distinguishable on screen, so this is now a check rather than two readings of one state (2026-09-04):** each cause selects its own user-facing sentence — the slugs are never rendered — so the two causes must produce two **different** sentences under the same word `degraded`. Getting the same sentence twice means the cause is not reaching the component, which is a wiring failure the word alone cannot show
- **A socket that accepts and never answers is a different failure from a refused connection**, and it is the one that hangs `fetch` forever without a timeout. Produce it — Task 1.8.4's squatter is the recipe — and confirm the indicator reaches a state rather than staying on the last one. **Wait at least `API_TIMEOUT_MS`**, which Task 1.12.2 set at 5 s and which the hook passes through unchanged: before that elapses the correct behaviour is indistinguishable from a slow answer. Note the next poll then starts 30 s after the deadline expires rather than 30 s after the request began, because Task 1.12.3 schedules on **settle** rather than on a clock — so a run of hung polls is one request every 35 s, not one every 30. **The state it must reach is `unreachable` and not `degraded`**: latency is deliberately not degradation, so a timeout means nothing arrived. That is an assertion rather than an observation, and it is the one that fails if the deadline was never wired
- **Watch recovery rather than inferring it.** Stop the backend, see the state change, start it again, and see the state return with **no page reload**. Prove it was not a reload the cheap way: `performance.timeOrigin` unchanged and exactly one `navigation` entry, which is the same check this repository uses for HMR
- **Confirm the last successful check time is right and survives — and know that it is deliberately invisible while things are working (2026-09-04).** It must not clear on a failed poll, must not advance on one, and must read as the time of the last _successful_ check rather than the last attempt. That is one line of code and the most likely thing in this story to be quietly wrong. **Task 1.12.4 shows it in every state except `healthy`**, where it would be a second copy of a fact the word already carries, so the check is: note the clock, stop the backend, and confirm the time that **appears** is the last success before you stopped it — not that a time is visible on a healthy page, where its absence is correct. There is a third rendering to produce as well and it is the one a wrong address gives: a client that has never once been answered says **"No successful check yet."** rather than leaving a gap, which is reachable by pointing `VITE_API_BASE_URL` at a dead address before the first load
- **Count the log lines rather than estimating them, and note the window has to be longer than a minute now.** The rendered floor is 2 lines per healthy request and 3 per 404 since Task 1.8.2. Task 1.12.3 set the interval at **30 s**, so a one-minute window catches about two polls and its error bars are wider than the thing being measured — take **five minutes** with one tab open, and again with several, and divide. The predicted figure to check against is 2 requests and 4 rendered lines a minute per visible tab. Record it beside the interval, so the choice can be re-read against evidence later
- **The tab has to be visible for any of that to happen, and Task 1.12.5 found the recorded version of this understates it (2026-09-04).** Task 1.12.3 stops the loop while `document.visibilityState` is `hidden` and polls immediately on return, and this file said an automated tab therefore "records one poll and then silence". **Measured, it records _no_ poll at all**: `poll()` is itself guarded by `isHidden()`, so a tab that is hidden **at mount** never makes the first request either — the chrome sits on Task 1.12.4's `checking` placeholder indefinitely, which is what a page opened in a background tab genuinely shows. Correct behaviour, and it is the single biggest trap in this task, because "the indicator is stuck on `checking`" is exactly what a broken wiring would look like. What Task 1.12.5 used to get past it, and what this task should reuse: `Object.defineProperty(document, "visibilityState", { configurable: true, get: () => "visible" })` followed by dispatching a `visibilitychange` event, which polls immediately. Keep the page visible for the whole measurement window, say in the record how you made it visible, and take the hidden reading deliberately. **The hidden reading was already taken once and reproduced exactly what the code promises — 0 requests across a hidden window, exactly 1 on return, no burst** — so this task's job there is a second reading over a longer window rather than a first one
- **Look at what a page load renders for its first round trip. Task 1.12.5 saw it in the assembled chrome and it holds, so this is a timing reading rather than a first sighting (2026-09-04).** Scoped to the assembled `<header>`, the placeholder measures **0 violations, 11 passes, 0 inconclusive** and its label is `--ink-secondary` `#5a5d5c` at 12px on the page ground — **5.99:1**, against the 4.5 threshold and against the 2.09:1 that got `--ink-disabled` rejected in Task 1.12.4, reproduced here for the comparison. The strip puts it on the same ground the workshop did, so that fix transferred rather than needing re-taking. What is left for this task is the duration. Task 1.12.4's `hasChecked` placeholder — a dashed marker and the word `checking` — is on screen from first paint until the first poll settles, on **every** load and every hard refresh, and it is the one rendering in this story whose duration is a round trip rather than a poll interval. Two things to confirm rather than assume: that it is brief enough not to read as a stuck state on a healthy pair, and that it does **not** reappear on navigation, which would mean the effect is being re-established — the same fact the request-counting bullet below tests, arriving somewhere visible. Against a **dead** backend it lasts the full `API_TIMEOUT_MS` of 5 s before becoming `unreachable`, which is correct and worth seeing once so it is not later mistaken for a hang
- **Take the re-render cost against the real tree, because the unit measurement is already in and it is not zero. Task 1.12.5 did _not_ confine it, deliberately, so the whole tree is what re-renders (2026-09-04).** Task 1.12.3 measured 4 polls producing 5 renders of the hook's caller — one initial and one per poll — because a successful poll writes a fresh `lastSuccessAt`, so the state object is new and React cannot bail out. `App` is the router's host and the call stayed there, so **every 30 seconds re-renders `AppHeader`, `<Routes>`, the current route, all four `Region`s and the render check's 36-row table.** The reasons are written beside the call and the reversal trigger is a second consumer or Epic 3's rate, so this is a number to take rather than a decision to re-open. Measure it where it actually lands rather than in a hook harness, and read it against the "no routine main-thread task >50 ms" target: at this size it should be invisible, and the number is worth having because Epic 3 inherits the shape at a much higher rate
- **Re-check the `ignore: "reqId,pid"` reversal trigger, because it is explicitly this story's — and note the evidence base moved under it.** `reqId` was kept in the `pretty` rendering precisely because this story is what makes requests interleave. **At a 30-second interval from one visible tab they largely will not**: two requests a minute, seconds apart from nothing else, is not interleaving, and the trigger was written when the interval was an open question. So the honest reading is either several tabs at once or the acknowledgement that the predicted condition did not arrive. Look at the terminal: if they do not in fact interleave, the lever is worth **172 → 117 columns**, which is the difference between a request line wrapping and not. Decide it on what the terminal actually looks like
- **Take the frontend's own cost.** ~~Whether the poll produces a re-render per tick regardless of whether anything changed~~ — **that half is answered and it does; see the bullet above, and what is left is measuring it against the real tree** — and whether the effect survives a route change without tearing down and re-establishing. The second is a genuine question with a specific answer to check: the effect's dependency array is `[intervalMs, timeoutMs]`, both of which are constants, and the hook is called from `App`, which is outside `<Routes>` — so a route change should re-render without re-running the effect, and a poll **should not** restart on navigation. Confirm it by counting requests across four navigations rather than reasoning about it, because a re-established effect is a request per route change and would be invisible except in the log
- Re-take the artefact figures — modules, JavaScript bytes, CSS bytes, file count and the md5 of each. **Task 1.12.5 moved both, and the stylesheet moved further than the bundle did, which is the reading that would be missed (2026-09-04):** wiring the component made it reachable from `index.html` for the first time, so the JavaScript went 346,144 → **348,124 B** (`d280e167…`) and the CSS **11,048 → 12,128 B** (`134d5dd8…`) — the +1,080 is the whole of `BackendIndicator.module.css` entering the artefact, not the one `.serviceRegion` rule this task added to the header. `index.html` is 1,101 B at yet another hash (`177df27d…`), for **361,653 B over four files** on a default build. ~~The frontend's bundle has moved once in seven stories and this story moves it again.~~ **That framing is stale and the correction is the interesting part (2026-09-04):** this story has moved the JavaScript **three** times already — Task 1.11.5's probe, Task 1.12.2's client and Task 1.12.3's hook, the last of them **downwards**, because deleting seven console branches costs more than a polling loop adds. And **Task 1.12.4 moved the _stylesheet_ for the first time since Task 1.7.6 while leaving the JavaScript byte-identical**: three `--service-*` tokens took the CSS 10,926 → 11,048 B at 346,144 B / `3d03f7dd…` unchanged, because `BackendIndicator` is reachable only from a stories file. So **read all four numbers rather than the bundle**, and expect Task 1.12.5 to be the change that finally moves the JavaScript, since wiring is what makes the component reachable from `index.html`. **Say which build produced them**: a default build and a deploy build differ in two files, the JavaScript by 72 bytes and `index.html` at an identical 1,101 B because it carries the hashed script filename — and note `index.html` moves when the hashed **stylesheet** filename changes too, which is how Task 1.12.4 changed it while adding no JavaScript at all. A size comparison reports the document unchanged in both cases and it is not

## Done when

- All three states have been seen locally, each from a named cause, with the reading recorded — **and both degraded causes seen, producing two different sentences**
- The not-yet-checked placeholder has been watched on a real load, and the never-succeeded sentence produced once from a dead address
- Recovery has been watched happening, with `timeOrigin` and the navigation count proving it was not a reload
- The log-line cost of the chosen interval is a measured figure rather than an estimate, taken over a window long enough for the 30-second interval and with the page **visible** for all of it
- The hidden-tab behaviour has been seen once deliberately: no polling while away, exactly one request on return
- A route change has been shown not to restart the poll
- The `reqId` rendering question is decided on evidence and recorded
- `pnpm verify` passes and `pnpm ready` still answers correctly for both the healthy pair and the half-pair

## Approach note

Every state in this story is a client-side conclusion, so the server log will say the system is healthy through most of what this task produces — and in the cross-origin case it will say **200 with a full body** while the browser says `TypeError: Failed to fetch`. The browser is the only instrument that can see what this story built. Take every reading there.
