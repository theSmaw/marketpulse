# Task 1.12.8 — Verify, document, and record ADR 0012

**Status:** Complete (2026-09-04)
**Story:** [1.12 Health & Status Vertical Slice](STORY.md)
**Depends on:** Task 1.12.7 — complete, and this file was amended against what Tasks 1.12.6 and 1.12.7 measured on 2026-09-04

## Objective

Close the story the way every Epic 1 story has been closed: re-running every criterion and re-taking every figure from a clean tree rather than inheriting them. **Closing the epic is not this task's** — Story 1.13 was added after this split and is the epic's last story, so Task 1.13.6 owns the exit criteria.

## Work

- **Re-run all six acceptance criteria against the shipping tree and the deployed environment.** Re-take every figure rather than citing one. The record across ten stories is that the half that is cheap to check has never been wrong and the half that cannot be checked has been wrong nearly every time it is read
- **Write `docs/adr/0012-*`.** It is the twelfth and it should record what this story decided rather than what it built: the client-side status vocabulary and why it is not `HealthStatus`; what "degraded" means and why that definition and not another — including the **reversal trigger** Task 1.12.1 attached to rejecting latency, which is a `/health` that does real work, and the consequence that a timeout is `unreachable`; **one indicator or two — decided by Task 1.12.4 and it is _two_**, on the argument that `FeedStatus` is a fact the backend _reports_ about the market feed while `BackendStatus` is one the client _concludes_ about whether the backend answered at all, that they fail independently, and that a live feed behind an unreachable backend is a real state one indicator would have to pick between; **what is shared is the marker language rather than the component, and the cost of that — two stylesheets holding one idiom by imitation — belongs in the record beside the decision**; **the fourth visual case that is deliberately not a fourth state**, which is `hasChecked` and the `checking` placeholder, and why rendering the honest `unreachable` there would report the client's own startup as a fact about the server while rendering nothing would collapse the region and be indistinguishable from a broken indicator; **whether a user sees which cause degraded the backend**, which is decided as a per-cause sentence rather than a slug or a `title` attribute, with the tooltip rejected for the reason Task 1.4.5 rejected one; **why the last successful check is an absolute hand-formatted clock time shown in every state except `healthy`**, and why `toLocaleTimeString` was rejected on a width that `tabular-nums` cannot fix; how much of a `requestId` a user sees — **which here is none at all, and structurally so, since no prop can carry one**; the polling interval and the three costs behind it — **including that it is bounded below by the client's five-second deadline, which ~~nothing checks~~ a test in `use-backend-health.test.ts` asserts since Task 1.12.3, and that the interval was deliberately kept a literal because a number coupled to another literal cannot be made configurable on its own without letting somebody invert an ordering from a file where the other half is invisible**; that a hidden tab does not poll — **and state it in Task 1.12.5's sharper form, because the weaker one was recorded first and is wrong**: a tab hidden _at mount_ makes no request at all, since `poll()` is itself guarded rather than only the scheduler, so a page opened in a background tab sits on `checking` indefinitely and is indistinguishable from broken wiring; **whether the hook's result travels as props or as context, decided by Task 1.12.5 as four props from `App` with the per-poll re-render of the whole tree accepted rather than confined** — the alternatives being a provider around the header alone, whose cost is `test-render.tsx` becoming the place every test gets the value from, and memoising the subtree, which trades one decision for a second one nothing checks, with the reversal trigger being a second consumer or Epic 3's rate; **why the third region is labelled for the service rather than the connection, and why it sits before the clock**; and what a hidden-tab loop costs anything trying to observe it; the client's seven outcomes and why they collapse to three states, since `api-error` and `http-error` differ only by whether a user has an id to quote and `aborted` is deliberately not a state; how the `requestId` decision constrains `ErrorFallback` without changing it; and what a green indicator does and does not certify — which is the same shape as ADR 0010's "what the tick certifies" and is the honest framing here, because every state in this story is a client-side conclusion. **Task 1.12.6 added two things to that record that are properties of the design rather than measurements of it (2026-09-04):** **`degraded` / `unreadable-body` is defined in terms of a host we do not control and has only ever been produced from one we wrote** — the obvious real producer, `vite preview`, gives a 404 to a JSON-accepting request because its fallback keys on `Accept` — so the ADR should say what the state is _for_ (a static host or a fallback answering at the API's address). **Task 1.12.7 closed the open half of that on 2026-09-04 and the answer is that there is a real producer**: Azure Static Web Apps' `navigationFallback` is a URL-pattern rule and **not** an `Accept` rule, so `/health` there answers **200 `index.html` in identical bytes at both `Accept: application/json` and `Accept: text/html`**, and pointing `VITE_API_BASE_URL` at the deployed frontend's own origin produced `unreadable-body` from a host nobody here authored for the first time. Write the ADR against that rather than against the hypothetical. **The transferable finding is not the sighting, it is that the two hosts disagree about the one request an API client actually sends** — `vite preview` 404s it and Static Web Apps serves the fallback — so "a static host answering at the API's address" is not one behaviour but two, and which state you get depends on the host's fallback rule rather than on anything this client does. **`not-ok-status` is equally reachable deployed** at `<origin>/assets`, which is excluded from the fallback and returns a real 404; measured by Task 1.12.7 and deliberately not spent a second deploy on, which is a decision the ADR should record rather than an omission. And **the seven-to-three collapse was verified from both sides rather than argued**: a 503 carrying an `ApiError` body and a 502 carrying an HTML page render identically, which is the collapse working, and it is worth stating that the distinction the client keeps internally is deliberately invisible to a user because only one of the two carries an id worth quoting. **Task 1.12.7 added two more that are properties of the design rather than measurements of it (2026-09-04).** **`unreachable` is one state with two latency signatures at opposite ends of the range**, which the ADR should say beside the "a timeout is `unreachable`, not `degraded`" consequence rather than leaving the reader to infer one cadence: a CORS rejection fails at the **round trip** (270–764 ms measured deployed) because the response _arrives_ and the browser discards it, while a hung socket fails at the **5 s deadline** and stretches the cycle to 36 s. So the fastest and the slowest failures this client can have both render the same word, and an operator timing one can tell them apart with no other instrument — which is the sort of thing Epic 3 will want for the feed. **And "what a green indicator does and does not certify" now has its evidence rather than only its framing**: with a wrong `CORS_ORIGIN` the browser reported `TypeError: Failed to fetch` while `curl` sending the same `Origin` got a **200 with the full body** and the backend's log recorded **38 requests, every one `statusCode: 200`**. Every state in this story is a client-side conclusion, and that table is what makes the ADR 0010-shaped framing concrete: a green indicator certifies that _this browser_ reached _that address_ and understood the answer, and nothing else — in particular not that the server is healthy, because in that reading it was
- **Update `CLAUDE.md` and `README.md` together.** `README.md` documents what a correct first run looks like and names five things that read as faults and are not — the `DISCONNECTED` feed indicator among them. This story changes at least one of those five, and possibly what the list means. `CLAUDE.md`'s artefact paragraph, its test counts, its `pnpm verify` timings and its coverage table all move. **Two specific things Task 1.12.4 put in the README's path (2026-09-04):** the five-things-that-read-as-faults list gains the `CHECKING` placeholder every load renders for a round trip, which is exactly the kind of correct-but-alarming thing that list exists for; and the list's `DISCONNECTED` feed entry now sits **beside** a second indicator reporting a healthy backend, so the entry has to say which of the two it means or a reader will match it to the wrong one. **Task 1.12.5 made both of those real rather than prospective (2026-09-04):** the strip a first run actually shows is three regions — `MARKET FEED / DISCONNECTED`, `BACKEND SERVICE / CHECKING` then `HEALTHY`, and `MARKET CLOCK / --:--:-- ET` — so the README's description of the chrome is stale in its own right, not only its faults list. **And there are now two `checking` figures, which is a trap for whichever one gets quoted (2026-09-04):** the placeholder clears in **50.7 ms** against a local pair and **283.2 ms** deployed, of which 267 ms is the public-internet round trip. `README.md` documents a **local** first run, so 50.7 ms is the figure that belongs there and the deployed one belongs nowhere near it — but say which environment each figure is from wherever either appears, because this file already records four cases of a figure that was right when taken and wrong one task later
- **Run the duplicated-sentence sweep, and run it with a `grep` rather than from memory.** Two stories have now found this class of drift: Task 1.9.7 found a warning standing in **thirteen** places rather than the three that were recorded, and Task 1.10.8 found **eight** copies of a claim that had been false since Task 1.10.5 and amended only in the copy being edited. The eleven "Conventions from Story 1.1" blocks are the usual site and are **known stale by two increments before you start**: they say **103** tests, the tree said 118 from Task 1.10.5, **137** after Task 1.12.1 took `packages/shared` from 7 across 2 files to 26 across 4, **160** after Task 1.12.2 took it to 37 and `apps/frontend` to 74 across 11, **170** after Task 1.12.3 took `apps/frontend` to 84, **183** after Task 1.12.4's status indicator took it to 97 across 12, and **189** after Task 1.12.5's wiring took it to 103 — so the figure was stale by two increments before this task existed and by **six** before it starts. **Tasks 1.12.6 and 1.12.7 both shipped no source and no tests, so the count is still 189 and the arrears are six (2026-09-04)** — 1.12.7 in particular changed only three Markdown files, so the artefact, the timings and the coverage table should all reproduce their Task 1.12.5 values, and any movement in them is a finding rather than an expected increment. Re-count it rather than citing this sentence. Amending only this story's copy is the exact failure the sweep exists for. and they are not eleven copies of one sentence — nine are byte-identical and Stories 1.2 and 1.3 carry their own historical strike-throughs, so compare hashes rather than diffing. Strike through rather than delete. **One site is already known to have been amended and should be checked rather than re-amended (2026-09-04):** Task 1.12.6 corrected `CLAUDE.md`'s `vite preview` paragraph, which had said the SPA fallback answers _any_ unmatched path with `index.html` and a 200 — it keys on the `Accept` header, and `application/json` gets a 404. That claim was stated in one place and repeated in substance in Story 1.5's deep-linking discussion, so `grep` for it rather than assuming the one edit covered it — **and note Task 1.12.7 amended that same paragraph a second time**, adding the Static Web Apps contrast, so it has now been edited by two consecutive tasks and is the paragraph most likely to have been left half-corrected. **Task 1.12.7 refined two further claims that stand in many places, and both counts below were taken with a `grep` rather than remembered (2026-09-04).** The **frontend's non-atomic upload window** — Task 1.11.7's ~2 s holding two broken states — is now scoped rather than falsified: it is a property of **the artefact changing**, because 174 consecutive CDN samples across a whole `Deploy the frontend` step of a docs-only merge showed **zero** broken states, a byte-identical rebuild keeping the hashed filenames and so having no incoming/outgoing pair. That claim stands in **14 files** — `README.md`, `CLAUDE.md`, `docs/adr/0011-*`, `.github/workflows/deploy.yml`, four Story 1.11 task files plus its `STORY.md` and `HOSTING.md`, and two Story 1.13 files — so it is the largest instance of this class the epic has met, and **the honest amendment is a scoping clause and not a strike-through**, because the window is real on a changing artefact and Task 1.12.7 did not re-test it there. Say so where you amend, or the next reader will take "zero broken states" as a refutation. And **Task 1.11.7's "1–4 requests per 30 s" probe-only baseline is now a precise and explainable 4** — liveness at `periodSeconds: 30` is 1 and readiness at `10` is 3 — standing in **4 files**, one of which is [Story 1.13's Task 1.13.5](../story-13-end-to-end-browser-testing/TASK-05-post-deploy-browser-check.md), so amending only this story's copies repeats the exact failure this sweep exists for
- **Sweep for claims this story falsified**, and expect them where a story wrote "Story 1.12 will". Candidates named in the story files: ~~`AppHeader`'s hard-coded `feedStatus="disconnected"` comment~~ — **the note pointing at this story was replaced with the decision by Task 1.12.4 in both `AppHeader.tsx` and `App.tsx`, so what is left to sweep is every _other_ place that says the vocabulary question is open**, `packages/shared/src/backend-status.ts`'s "the visual language is a separate decision" paragraph among them, and note that `feedStatus="disconnected"` itself is still correct and stays until Epic 3; ~~`health-probe.ts`'s and `main.tsx`'s own deletion notes~~ — **both discharged by Task 1.12.3, which deleted the module and the line; what is left to sweep is every _other_ place that says the frontend calls the backend once at startup, and `README.md`'s five-things list, which that task amended to six and which this task should re-count rather than trust**; `report-error.ts`'s window-listener trigger — **already re-taken and re-recorded by Task 1.12.2 as a condition (an endpoint that accepts a client error report) rather than a story number, so verify it rather than deciding it again**; ~~the `ignore: "reqId,pid"` lever~~ — **decided by Task 1.12.6 and the answer is no, so verify it rather than deciding it again (2026-09-04)**: 51 request pairs across two windows, every one adjacent, none split, including 30 from three tabs at once, so the interleaving the trigger predicted never arrived; the lever reconciles at **156 → 101 columns**, which is Task 1.8.2's 172 → 117 once pnpm's line prefix is added back, and the trigger is re-stated as Epic 3's socket rather than retired; the "fifteen error-level rules have never fired on shipped code" claim, in every place it stands — **and note Task 1.12.3 made this one subtler rather than false: the repository now has stateful shipped code and the rules still said nothing, because the shape they object to is a _synchronous_ update in an effect body, so any copy reading "because nothing here has state" is now wrong for the stated reason while right in its conclusion**; the `verify`-gap entry that says the frontend's variable pair is one variable long and the two agree — **still true, because Task 1.12.3 kept the poll interval a literal and added no variable, so confirm it rather than amending it**; and ~~`App.tsx`'s note that its `useBackendHealth()` result is discarded for exactly one task~~ — **removed by Task 1.12.5, so verify it rather than looking for it; what replaced it is the call-site and re-render decision, which is prose this task should check against the code rather than trust**
- **Record two smaller things Task 1.12.4 changed that no criterion will catch (2026-09-04).** `apps/frontend/src/styles/market.css` now holds a **second** block that is not about the market — `--service-*` beside `--status-error` — and the reason the trio duplicates the `--feed-*` values rather than aliasing them is that the two indicators report facts that fail independently, so one changing colour must not move the other. And the workshop produced its **first real `color-contrast` violation** rather than the standing `aria-hidden`-glyph inconclusive: `--ink-disabled` at 12px on the page ground measures **2.09:1** against a 4.5 threshold. Both belong in `CLAUDE.md`, because the first is a rule about where colour lives and the second falsifies a sentence that file has carried since Task 1.4.5. **And one from Task 1.12.5 in the same category**: `AppHeader.module.css` now carries a `max-width: 34ch` on the service region, which is the first length in a component stylesheet that is neither a token nor a grid fraction. It is deliberate and it belongs recorded as an exception with its reason — a measure in the text's own units, because that region is the only one of the three that renders two sentences under its word and the strip is the header grid's `auto` column — or the next reader will take it as licence for literal lengths generally
- **Check the epic's fourth exit criterion — "backend health/status can be viewed from the frontend" — against the tree rather than against this story's status, and record it as met on the evidence.** It is the criterion this story exists for and the last of the four to close. Checking all four and closing the epic is **Task 1.13.6's**, and it will read what you write here rather than re-deriving it. **The deployed half of that evidence already exists and is dated: Task 1.12.7 saw all three states in a browser against the live pair on 2026-09-04**, each from a named cause, with the environment restored afterwards and the restoration proved by the deployed bundle returning to its exact pre-probe md5. Cite it as the deployed evidence and keep it **separate** from the local evidence, which is Task 1.12.6's — the criterion's wording is "viewed from the frontend" and this story has now met it twice over in two environments, which is worth stating as two readings rather than collapsing into one
- **Hand over deliberately, and there are now two audiences.** The connection-state pattern this story establishes is what Epic 3 reuses for the live market feed, and the open questions it leaves — the idle-billing condition, whether the interval survives contact with several tabs, whether `FeedStatus` and this vocabulary stay separate — belong written down where the next epic will meet them. **Story 1.13 is the nearer audience**: its whole justification is the failure this story makes shippable, so the evidence Task 1.12.7 gathered by hand is what it builds its check against — **and that hand-over is already written, as a string set rather than a description**, at the end of [Task 1.12.7's file](TASK-07-verify-against-the-deployed-environment.md): the four words on screen including the `checking` placeholder a check must wait past, the three region labels, the four detail sentences verbatim, the visibility precondition without which the loop never starts, and the three-instrument table as the check's acceptance criterion. Point at it rather than restating it, and check it against the code rather than trusting it. **Two of the three open questions handed to Epic 3 have moved (2026-09-04).** Whether the interval survives contact with several tabs is **answered**: the deployed cost is a step function of **4 → 5 → 6 requests per 30 s for 0, 1 and 2 visible tabs**, so one visible tab adds exactly +1 per 30 s and the polling criterion is met on measurement rather than on argument. The **idle-billing condition is still open and its refusal has changed shape**, which is what to hand over rather than the old cause: `az consumption usage list` now returns **`[]` at exit 0** rather than refusing with _"doesn't have valid WebDirect/AIRS offer type"_, and the Cost Management query API answers **`429`**. Anyone re-taking it in Epic 2 or Epic 3 should not cite Task 1.11.8's reason, because it is no longer what happens. The estimate itself reproduces to the cent at **$9.21/month idle and $19.04 active** against a **$20** budget that therefore still would not fire on the change that matters most — which is the one number in this area worth repeating, because it is the one that is wrong in the dangerous direction
- Re-run the full chain from a **clean clone** with an empty pnpm store, as every closing task in this epic has: install, `pnpm verify`, the per-step split, the artefact hashes, and the test counts across all four suites

## Done when

- All six criteria are met, each re-verified rather than inherited, with the deployed evidence separate from the local evidence
- `docs/adr/0012-*` exists and records decisions rather than a changelog
- `CLAUDE.md` and `README.md` agree with the tree, and the duplicated-sentence sweep has been run with a `grep` and its count recorded
- The epic's fourth exit criterion is checked against the tree and recorded with its evidence
- `pnpm verify` passes from a clean clone, the pipeline is green, and the deployed environment is up and correct

## Approach note

Every closing task in this epic has found at least one recorded claim that had stopped being true, and the ones that were found were found by re-measuring rather than by reading. Task 1.11.8 found three, including a paragraph that contradicted itself two sentences apart and an arithmetic error of a factor of about 35. Budget for finding some here; the cheapest place to find them is the task that is looking.

## What was measured (2026-09-04)

Every figure below was re-taken rather than cited. The local and deployed
evidence are kept separate deliberately — the criterion is "not only locally".

### The six acceptance criteria

| #   | Criterion                                              | Local                                                                                              | Deployed                                                                        |
| --- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| 1   | Queries health, displays it in the chrome              | **Met** — `Market feed / disconnected`, `Backend service / healthy`, `Market clock / --:--:-- ET`  | **Met** — `Backend service / healthy` on the live pair                          |
| 2   | Distinguishes healthy, degraded, unreachable           | **Met** — all three, and both `degraded` causes rendering different sentences under one word       | **Met** — all three (Task 1.12.7); `unreadable-body` producer re-confirmed here |
| 3   | Unreachable reports last check; interface stays usable | **Met** — `Last confirmed 10:21:16`; five routes navigate, **0** fallbacks, **1** navigation entry | **Met** (Task 1.12.7)                                                           |
| 4   | Recovery automatic, no reload                          | **Met** — watched, table below                                                                     | **Met** (Task 1.12.7)                                                           |
| 5   | Verified against the deployed environment              | —                                                                                                  | **Met** — healthy, poll, correlation ids, fallback rule, all re-taken           |
| 6   | Polling deliberate, does not spam logs                 | **Met** — 6 polls / 185 s, 2 log records each, 0 header mutations, 0 long tasks                    | **Met** — cycle 31.05 s, round trip 262–268 ms                                  |

### Recovery, watched rather than inferred

| Time     | Event                                                         |
| -------- | ------------------------------------------------------------- |
| 10:23:20 | `healthy`                                                     |
| 10:23:28 | backend `SIGTERM`ed — the poll at that second still succeeded |
| 10:24:00 | `unreachable`, holding `Last confirmed 10:23:28`              |
| 10:24:05 | backend answering again                                       |
| 10:24:31 | `healthy` again                                               |

`performance.timeOrigin` unchanged at `1788488462798.1`, **one** navigation entry
of type `navigate`, on `/replay` — a route reached by client-side navigation.
Zero error fallbacks throughout. The failed poll neither cleared nor advanced the
timestamp.

### The poll (local, 185 s, one visible tab)

6 requests; cycle **30.98 / 31.00 / 31.00 / 31.00 / 31.02 s**; **12 log records,
exactly 2 per request**; round trip 7.8–25.1 ms; **0 header DOM mutations** and
**0 `longtask` entries**. The 31 s rather than 30 is Chrome's timer alignment in a
genuinely backgrounded automated tab, not the application.

### The deployed pair

- Three correlation ids followed from a browser to a Log Analytics record —
  `responseTime` **0.48 / 0.31 / 0.38 ms**, `pid` 1, revision `0000040`
- Round trip **250.8 / 262 / 268.1 / 275 / 385.5 ms**
- An automated tab sat on `CHECKING` for **twelve seconds** making no request
  until `visibilityState` was overridden — Task 1.12.7's precondition, reproduced
- `/health` **200 `index.html` at 1,101 B under both `Accept` values**;
  `/assets/health` **404** at both — Task 1.12.7's `navigationFallback` finding,
  reproduced exactly
- Deployed bundle still `index-CL7CW2na.js` — Task 1.12.7's restored value

### The tree

`pnpm verify` **exit 0 in 27.06 s** from a clean clone after **398 packages cold
in 7.91 s**; warm **21.93 s** (build 2.44 / lint 4.03 / `format:check` 4.05 /
`stories` 0.27 / `env:check` 0.29 / `test` 3.38 / `test:process` 7.79). **189
tests** (37 / 49 / 103) plus **10** process tests. The artefact reproduces Task
1.12.5's figures **to the byte** — **361,653 B over four files at 278 modules**.

### The sweep, run with a `grep`

- The convention block is **twelve** now, not eleven — Story 1.13 added one — and
  **ten** are byte-identical rather than nine.
- The current-tense `103 real tests` claim stood in **12 sites** (10 convention
  blocks plus two in `EPIC.md`), stale by **six** increments. The other **27**
  occurrences of "103 tests" across the tree are historical records correct in
  their own context — the distinction a grep-and-replace would have destroyed.
- The **non-atomic upload** claim stands in **14** files; all 14 now carry the
  scoping clause, as a **scoping clause and not a strike-through**, because the
  window is real on a changing artefact and was not re-measured there.
- The **"1–4 requests per 30 s"** baseline stands in **4** files; all four refined
  to a precise and explainable **4**.

### Four recorded claims that had stopped being true

1. **`docs/adr/0011-*` §23** described `apps/frontend/src/health-probe.ts` in the
   present tense two tasks after Task 1.12.3 deleted it.
2. **`docs/adr/0009-*`** and **Story 1.7's `STORY.md`** both said the React
   Compiler rules had never fired "because nothing shipped here has state". That
   is **wrong in its stated reason and right in its conclusion**: the repository
   now ships state, an effect and a network loop, and all seventeen rules were
   still silent — because `set-state-in-effect` objects to a _synchronous_ update
   in an effect body, which is not that hook's shape.
3. **`CLAUDE.md`'s coverage table** was stale for two of three packages — shared
   30.00% → **75.86%**, frontend 68.25% → **85.80%** — while the backend
   reproduces to the digit, which is exactly what a story that added tests to two
   packages should look like.
4. **`CLAUDE.md` said "All twelve" ADRs when there were eleven.** An off-by-one
   that this task makes accidentally true. It was found by counting the directory
   rather than by reading the sentence, which is the whole method.

`README.md`'s "what looks broken" list is **seven** items now, not five or six:
the `CHECKING` placeholder is a new one, and the `DISCONNECTED` entry had to say
which of the two indicators it means.

## Done

Story 1.12 is closed. `docs/adr/0012-*` is written, the epic's fourth exit
criterion is recorded as met with its evidence for Task 1.13.6 to read, and
Story 1.13 has its handover verified against the code rather than trusted.
