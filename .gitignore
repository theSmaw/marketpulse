node_modules/
dist/
build/
coverage/

# Composite projects write this next to the tsconfig, not into dist/, so the
# rule above does not catch it.
*.tsbuildinfo

.env
.env.*
!.env.example

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
.tmp/
tmp/
temp/
